{% extends 'base2.html.twig' %}

{% block title %}BestelRegel{% endblock %}

{% block body %}
<h1>BestelRegel</h1>
<hr>
<table class="table">
    <tbody>
	        <tr>
            <th>Id</th>
            <td>{{ bestel_regel.id }}</td>
        </tr>
	        <tr>
            <th>Aantal</th>
            <td>{{ bestel_regel.aantal }}</td>
        </tr>
	    </tbody>
</table>

<a href="{{ path('bestel_regel_index') }}" class="btn btn-primary">Terug</a>
<br>
<a class="btn btn-warning" href="{{ path('bestel_regel_edit', {'id': bestel_regel.id}) }}">Bewerken</a>
<br>
{{ include('bestel_regel/_delete_form.html.twig') }}
{% endblock %}
