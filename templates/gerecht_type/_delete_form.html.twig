<form method="post" action="{{ path('gerecht_type_delete', {'id': gerecht_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gerecht_type.id) }}">
    <button class="btn btn-danger">Verwijderen</button>
</form>
